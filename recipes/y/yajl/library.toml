title = "yajl"
version = "2.1.0"
description = "YAJL (Yet Another JSON Library) is a small, event-driven JSON parser and generator for C. It provides streaming parsing for large JSON documents and is used by many applications."
license = "MIT"
dependencies = "cmake"
canonical_name = "yajl"
recipe_url = "https://github.com/lloyd/yajl/archive/refs/tags/2.1.0.tar.gz"
