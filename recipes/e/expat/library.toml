title = "expat"
version = "2.6.4"
description = "Expat is a stream-oriented XML parser library written in C. It is widely used for parsing XML documents, particularly in projects like Firefox, Apache HTTP Server, and Python's xml.etree module."
license = "MIT"
dependencies = "None (pure C library)"
recipe_url = "https://github.com/libexpat/libexpat/releases/download/R_${VERSION//./_}/expat-2.6.4.tar.gz"
