title = "c-ares"
version = "1.34.4"
description = "c-ares is a C library for asynchronous DNS requests and name resolution. It provides non-blocking DNS lookups and is used by curl, Apache Mesos, and many other projects requiring asynchronous name resolution."
license = "MIT"
dependencies = "cmake (build)"
recipe_url = "https://c-ares.org/download/c-ares-1.34.4.tar.gz"
